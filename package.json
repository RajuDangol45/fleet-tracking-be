{
  "name": "fleet-tracking-platform",
  "version": "0.0.0",
  "scripts": {
    "api": "json-server --watch db.json --port $PORT --host 0.0.0.0"
  },
  "private": true,
  "packageManager": "npm@10.9.2",
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
  },
  "devDependencies": {}
}
