{
  "name": "fleet-tracking-platform",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "api": "json-server --watch db.json --port $PORT --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
